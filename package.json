{
  "name": "shytalk",
  "private": true,
  "scripts": {
    "test:web": "playwright test",
    "seed:dev": "node scripts/seed-dev-fixtures.mjs",
    "prepare": "husky"
  },
  "lint-staged": {
    "express-api/**/*.js": [
      "npx --prefix express-api eslint",
      "npx --prefix express-api prettier --check"
    ],
    "*.feature": [
      "bash .claude/hooks/check-gherkin.sh"
    ],
    "*.{kt,kts}": [
      "bash -c 'if command -v ktlint &>/dev/null; then ktlint --relative; elif [ -f ./gradlew ]; then ./gradlew ktlintCheck detekt --daemon 2>/dev/null || echo \"⚠ Kotlin lint failed — fix before pushing\"; else echo \"⚠ Kotlin lint not available locally — CI will catch issues\"; fi'"
    ]
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "allure-playwright": "^3.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "sharp": "^0.34.5"
  }
}
