---
id: SHY-0038
status: Draft
owner: claude
created: 2026-06-08
priority: P0
effort: L
type: infra
roadmap_ids: []
epic: EPIC-0001
pr:
---

# SHY-0038: Full bidirectional sync — SHY `.md` files → `public/roadmap-data.json` auto-regen + GitHub Project board link on the public roadmap page

## User Story

As **(a)** a ShyTalk maintainer and **(b)** an outside contributor / observer of the project, I want **the public `shytalk.com/roadmap` webpage to be a derived view of the SHY `.md` corpus (auto-regenerated by CI whenever a SHY status flips) AND to link to the canonical ShyTalk Stories GitHub Project board** so that the two operator-mandated surfaces (public webpage + GH Project board) stay in lockstep with the `.md` source of truth, never drift, and never require a human to remember to update them.

## Why

[[feedback-stories-epics-and-two-surface-sync]] HARD GLOBAL rule (operator 2026-06-07 ~20:48 BST): "every SHY status flip MUST sync to BOTH the public roadmap webpage on shytalk.com AND the GitHub Project board on the repo page; sync MUST be automated (CI regen webpage on merge; SHY-0002 mirror updates Project board); never manual-edit the surfaces."

Today: the SHY-0002 mirror handles the GH Project board side. The webpage side is hand-maintained — a status flip in a SHY `.md` does NOT propagate to `public/roadmap-data.json`. That breaks the rule's _automated_ clause and exposes us to silent drift.

This SHY closes the webpage half of the sync, plus adds the missing cross-link from the webpage to the GH Project board. It absorbs the previously-reserved SHY-0039 (whose entire scope was "CI workflow for SHY → roadmap-data.json auto-sync") because the operator-explicit prioritisation of **quality + reliability + free cost over speed** (2026-06-08 ~18:38 BST) makes the two-PR split a quality compromise: between SHY-0038 ship and SHY-0039 ship the rule would be half-true, and the schema fields added by a smaller SHY-0038 would be dead-code until SHY-0039 catches up.

**Scope alternatives explored (recorded for architect/operator review):**

- **Option A — Link only.** Footer link to GH Project. No JSON change. Pros: zero regression risk; instant. Cons: doesn't deliver the sync rule; forces a schema migration next SHY.
- **Option B — Link + dead schema.** A + optional `epicId`/`shyId` JSON fields with no consumer. Pros: small impl. Cons: schema fields rot until SHY-0042 grouping UI lands; doesn't deliver the sync rule. _(Was the prior recommendation when scope was speed-optimised; rejected under the new quality-first criterion.)_
- **Option C — Link + schema + EPIC grouping UI.** B + render EPIC badges + grouping toggle. Pros: visible EPIC concept. Cons: doesn't deliver the sync rule; visual regression risk on the existing layout.
- **Option D — Full bidirectional sync (CHOSEN).** A + JSON schema extension + CI workflow that regenerates `roadmap-data.json` from SHY `.md` frontmatter on every push-to-main that touches `.project/stories/**`. Pros: literal rule delivery; single source of truth; no manual sync drift possible. Cons: bigger PR (~800-1000 LOC); requires "what's public?" filter rule design.
- **Option E — Live GH Issues API at page-load.** No JSON file; webpage fetches on render. Pros: always fresh. Cons: anonymous GH API rate limit (60 req/hr/IP) breaks the page at scale; depends on GH API uptime; rejected on reliability grounds.
- **Option F — Hybrid live + cached.** E + D's static JSON as fallback. Pros: highest theoretical reliability. Cons: doubles test surface; YAGNI — D's auto-regen already keeps JSON ~30s fresh after any push, so the live path adds complexity for negligible freshness gain.
- **Option G — GH-native README/wiki.** Auto-generated wiki page; shytalk.com just links to it. Pros: zero JS complexity. Cons: visitors leave shytalk.com to see the roadmap detail; SEO hit; abandons the polished website aesthetic.

Operator decision (2026-06-08 ~18:38 BST): Option D, optimising for quality + reliability + free cost over speed/effort.

**Two Day-0 design decisions resolved by the operator (same timestamp):**

1. **Filter rule** = opt-in via `public: true` SHY frontmatter (default = internal). Safer default — a new SHY can't accidentally leak internal content.
2. **Auto-commit loop guard** = `if: github.actor != 'github-actions[bot]'` on the sync workflow. Most robust — the workflow refuses to re-run on its own commit; no commit-message magic; clean audit trail.

## Acceptance Criteria

### Happy path

- [ ] `public/roadmap.html` footer gains a visible link element with text "View on GitHub Project" (i18n key `viewProjectBoard`) with `href="https://github.com/orgs/Shyden-Ltd/projects/1"`. Operator-confirmed URL (2026-06-08 ~18:38 BST).
- [ ] The link opens in a new tab (`target="_blank" rel="noopener noreferrer"`), is keyboard-focusable with visible focus ring matching existing link styles, and has an `aria-label` localised via the `viewProjectBoardAria` key.
- [ ] `scripts/sync-shy-to-roadmap-data.mjs` exists, is executable from `node scripts/sync-shy-to-roadmap-data.mjs`, parses every `.project/stories/SHY-NNNN-*.md` file's frontmatter, filters to SHYs with `public: true`, and writes `public/roadmap-data.json` with a stable, deterministic output (sorted by phase + priority + created date so noise diffs don't appear when nothing semantic changed).
- [ ] `public/roadmap-data.json` gains a top-level `_meta` block: `{ "schemaVersion": 2, "generatedFrom": ".project/stories/", "generatedAt": "<ISO-8601 UTC>", "shyCount": <N>, "epicCount": <M> }`. The existing `phases`, `currentlyWorkingOn`, `lastUpdated` keys are preserved (back-compat for the existing renderer).
- [ ] Each public-surfaced item in the JSON gains optional fields: `shyId` (e.g. `SHY-0038`), `epicId` (e.g. `EPIC-0001`), `status` (mirrors SHY frontmatter `status`), `prUrl` (mirrors SHY frontmatter `pr` when set). Items without a corresponding SHY (manually-curated phase items pre-dating the SHY framework) keep the existing shape, no new fields.
- [ ] CLAUDE.md § Agile Way of Working § "Public-surfacing" subsection (new) documents the `public: true` frontmatter convention + the auto-sync workflow + the opt-in default + how to flip a SHY's public-facing visibility.
- [ ] `.github/workflows/sync-roadmap-data.yml` runs on push to `main` when `.project/stories/**` paths change. Guard: `if: github.actor != 'github-actions[bot]'`. Steps: checkout, set up Node, run `node scripts/sync-shy-to-roadmap-data.mjs`, check `git diff --quiet public/roadmap-data.json` — if differences, commit + push back to main with message `chore(roadmap): sync roadmap-data.json from SHY corpus` and committer `github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>`.
- [ ] The actor-check guard verifiably prevents loops: a test (Jest or a workflow-syntax assertion) confirms the `if:` expression is present on the sync job.
- [ ] At least one existing SHY is opted-in (e.g. SHY-0038 itself gets `public: true` in its frontmatter as a worked example, OR an existing user-visible feature SHY if any qualify) so the regenerated JSON has at least one populated `shyId` field.
- [ ] i18n: `viewProjectBoard` + `viewProjectBoardAria` keys exist in EVERY locale already present in `public/roadmap.html`'s i18n table (EN + ar/de/es/fr/hi/id/ja/ko/pt/zh = 11 total).
- [ ] No regression on existing rendering: phases, items, donut chart, legend, sticky-nav, subscribe — all pixel-equivalent at 360px / 768px / 1280px breakpoints (Playwright visual diff).

### Error paths

- [ ] If a SHY `.md` has `public: true` but is missing required public-facing fields (e.g. no `title`, no `description` derivable from User Story), the sync script MUST fail loudly with a per-file error path + line + missing-field name; CI MUST exit non-zero; no partial JSON write.
- [ ] If `public/roadmap-data.json` becomes malformed (e.g. by a manual edit that the sync script then mis-handles), the existing webpage MUST still render the `_headers` skeleton/error state — page does not white-screen.
- [ ] If the GitHub API is unreachable during the sync workflow (rare; sync doesn't call API directly but Node setup might), the workflow fails the run, does NOT commit a partial JSON, and surfaces the failure on the PR/main check.
- [ ] If a SHY has `public: true` AND `status: Cancelled`, the script MUST emit it with `status: Cancelled` (not silently drop it) — public visibility is independent of cancellation.
- [ ] If TWO SHYs share the same `roadmap_ids` entry (e.g. both claim `G009`), the script MUST fail with a duplicate-claim error naming both file paths — prevents silent merge of distinct work into one roadmap item.

### Edge cases

- [ ] An EPIC with zero public-surfaced children: still emitted in `_meta.epicCount` but contributes nothing to phase items.
- [ ] A SHY with `public: true` but no `epic:` field: surfaced as a top-level item, no EPIC grouping (renderer treats as orphan; that's fine — EPIC grouping UI is SHY-0042's scope).
- [ ] A SHY flips `public: true` → absent (was public, now internal): next sync run REMOVES it from JSON. Renderer must not crash on an item disappearing between renders (browser refresh is the user's path).
- [ ] An EPIC file has `status: Cancelled`: its child SHYs still surface based on their own `public:` flag (an epic being cancelled doesn't auto-cancel the work).
- [ ] First-run on a corpus with zero `public: true` SHYs: script writes a JSON with empty `shyId` arrays in phases — does NOT crash; CI commits the (mostly-empty) `_meta` block as a baseline.
- [ ] Concurrent push race: two pushes to main land within seconds. Second sync run sees JSON already regenerated by the first; `git diff --quiet` returns no diff; no second commit. No infinite loop.

### Performance

- [ ] Sync script completes in <5s on the full SHY corpus (60+ SHYs + 1+ EPICs today; budget grows to <15s at corpus size 200).
- [ ] JSON file size increase: <50KB over current baseline (177KB) at corpus size 60; documented growth budget = ~1KB per public-surfaced SHY.
- [ ] CI sync workflow end-to-end (checkout + Node setup + script + diff + commit + push) completes in <90s on the standard GH-hosted runner.
- [ ] Page-load impact: no new network requests (link is `<a href>`; JSON growth is read-once); First Contentful Paint unchanged within ±50ms (Lighthouse baseline + post-change comparison in the PR description).

### Security

- [ ] `rel="noopener noreferrer"` mandatory on the new external link to prevent reverse tabnabbing.
- [ ] Sync script MUST NOT execute any frontmatter value through a shell (no `exec`, no `child_process.exec`); all parsing is pure-JS via `gray-matter` or hand-rolled regex (preference: hand-rolled, no new npm dep).
- [ ] The CI commit token (`GITHUB_TOKEN` with `contents: write` permission) is scoped to the sync workflow only; not exposed in env logs.
- [ ] No user-controlled string is interpolated into the link's `href` — it's a literal hardcoded URL in `public/roadmap.html`.
- [ ] CSP impact: existing `public/_headers` Content-Security-Policy must continue to allow the GH Project URL as a navigation target (HTTP nav, not iframe — no `frame-src` change needed; verify `connect-src` unaffected since the link is not a fetch).

### UX

- [ ] Link uses the existing primary-link colour + hover state (matches subscribe/footer styling).
- [ ] Mobile: link has ≥44px touch target (WCAG AA).
- [ ] Tooltip on hover (`title=` attr) is localised; explains "Opens the public GitHub Project board for ShyTalk Stories".
- [ ] When a SHY flips from `Draft` → `In Progress` → `Done` and lands in main, the public webpage updates within ~90s of the PR squash-merge (CI workflow runtime budget). The "lag" is documented in CLAUDE.md so contributors know what to expect.

### i18n

- [ ] Two new keys (`viewProjectBoard`, `viewProjectBoardAria`) added for EN + 10 existing locales. Machine translation OK for first pass; PR description flags for native-speaker review.
- [ ] No string concatenation in rendered text — full sentence is one i18n key.
- [ ] If the sync script ever emits SHY titles into JSON, those titles are i18n-keyed in the JSON (key = `shyTitle.<id>`) or labelled `i18nPending: true` so the renderer can fall back gracefully; for THIS PR, no SHY titles are emitted (the existing `phases[].items[].name` shape stays the canonical title).

### Observability

- [ ] Sync script emits a one-line summary to stdout: `[sync] regenerated public/roadmap-data.json — N public SHYs, M EPICs, diff: <bytes>` so the CI logs are immediately informative.
- [ ] On no-op runs (no diff), the script logs `[sync] no changes to public/roadmap-data.json` and exits 0 cleanly.
- [ ] The `_meta.generatedAt` timestamp + `_meta.generatedFrom` provenance lets a webpage-debugger figure out exactly which commit drove the current data (correlate with git log).
- [ ] No telemetry/analytics is added; consistent with the existing analytics-free roadmap page.

## BDD Scenarios

**Scenario: A SHY status flip propagates to the public webpage**

- Given SHY-0099 exists with `public: true` and `status: In Progress`
- When the SHY's status flips to `Done` and the change merges into `main`
- Then within ~90s the `sync-roadmap-data.yml` workflow runs
- And it regenerates `public/roadmap-data.json` with SHY-0099's status updated to `Done`
- And it commits the regen with committer `github-actions[bot]` and message `chore(roadmap): sync roadmap-data.json from SHY corpus`
- And the public webpage shows SHY-0099 as Done on the next browser refresh

**Scenario: A private SHY does NOT leak to the public webpage**

- Given SHY-0099 exists WITHOUT a `public:` field (default = internal)
- When its status flips and merges into main
- Then the sync workflow runs and does NOT include SHY-0099 in the regenerated JSON
- And the public webpage shows no trace of SHY-0099

**Scenario: Anonymous visitor sees the GH Project link**

- Given I navigate to shytalk.com/roadmap
- When the page finishes loading
- Then I see a footer link with text "View on GitHub Project"
- And the link's `href` is exactly `https://github.com/orgs/Shyden-Ltd/projects/1`
- And the link has `target="_blank"` and `rel="noopener noreferrer"`

**Scenario: Auto-commit loop does not occur**

- Given the sync workflow has just committed a regen to main
- When the commit lands and would normally trigger workflows on main
- Then the sync workflow's `if: github.actor != 'github-actions[bot]'` guard skips the run
- And no second sync commit is created

**Scenario: Malformed SHY frontmatter fails CI loudly**

- Given a SHY has `public: true` but is missing a required field (e.g. no `title`)
- When the sync workflow runs
- Then the script exits non-zero with stderr naming the file + the missing field
- And CI fails on the affected push
- And `public/roadmap-data.json` is NOT modified (no partial write)

**Scenario: Concurrent pushes do not double-commit**

- Given two SHY edits land in main within ~30s
- When the sync workflow runs for the second push
- Then it sees the JSON already up-to-date (first run committed it)
- And `git diff --quiet` returns no diff
- And no second commit is created

## Test Plan

- **Jest (`express-api/tests/scripts/sync-shy-to-roadmap-data.test.js` — NEW)**: ≥30 unit tests covering — frontmatter parsing (CRLF/BOM/array forms/optional fields), `public:` filter (true/false/absent), deterministic output sorting, `_meta` block shape + content, error paths (missing required fields, duplicate `roadmap_ids`, malformed status), edge cases (zero public SHYs, Cancelled status, missing `epic:` field), performance budget (60-SHY corpus <5s wall-clock).
- **Workflow assertion (`express-api/tests/workflows/sync-roadmap-data.test.js` — NEW)**: actionlint clean; the `if: github.actor != 'github-actions[bot]'` guard is present and well-formed on the sync job; path filter is exactly `.project/stories/**`.
- **Playwright (existing `tests/playwright/web/public-pages.spec.ts` extension)**: 5 cases — link present + correct href + opens-in-new-tab attrs + keyboard focus visible + RTL (ar) renders. Visual diff at 360/768/1280.
- **End-to-end integration test** (Bash, `scripts/test-sync-e2e.sh` — NEW or inline in the workflow): mutate a fixture SHY → run sync script → assert JSON diff appears → revert → run again → assert no diff.
- **Manual QA**: operator opens regenerated `roadmap.html` locally, tabs through to the new footer link, verifies open-in-new-tab, verifies the JSON `_meta` block looks right.

## Out of Scope

- **EPIC grouping UI in the webpage** — badge rendering, EPIC-as-filter, EPIC-as-group-header. Reserved for SHY-0042. THIS PR emits `epicId` into the JSON but does not render it.
- **Backfilling `public: true` across all existing SHYs** — only SHY-0038 itself (and 0–N operator-curated worked examples) get the flag here. Mass triage of "which existing SHYs are public?" deferred to a follow-up SHY (SHY-0060 reserved-id collision: check; if taken, use next free).
- **Write-side project automation** (auto-moving project cards on PR merge etc.) — outside the webpage-sync remit; would need `project` write scope on the bot.
- **Lighthouse perf budget enforcement in CI** — manual baseline + compare in PR description for this round; CI gate is future work.
- **Translation quality review** — machine translation for `viewProjectBoard`/`viewProjectBoardAria` is acceptable; native-speaker review can be a follow-up.

## Dependencies

- ✅ SHY-0037 (EPIC concept defined, validator landed; PR #1043 merged 2026-06-08 18:10 BST). Without this, `epicId` field has no semantic anchor.
- ✅ SHY-0002 (GH Issues + Projects v2 sync; PR #1035 merged earlier). Handles the OTHER half of the two-surface sync; this PR closes the webpage half.
- ✅ Operator-confirmed GH Project URL `https://github.com/orgs/Shyden-Ltd/projects/1` (2026-06-08 ~18:38 BST).
- ✅ Operator-confirmed filter rule = opt-in `public: true` (2026-06-08 ~18:40 BST).
- ✅ Operator-confirmed loop guard = `if: github.actor != 'github-actions[bot]'` (2026-06-08 ~18:40 BST).
- ⬜ **SHY-0039 (Reserved) — ABSORBED into this SHY.** Its entire scope ("CI workflow for SHY → roadmap-data.json auto-sync") is delivered here. The reservation slot is repurposed in SHY-INDEX as an open slot for the next concern.

## Risks & Mitigations

- **Risk: auto-commit loop despite the actor guard.** Some workflow triggers (e.g. `workflow_run`) ignore `github.actor`. Mitigation: pin the trigger to `on: push` only, never `workflow_run`; include the path filter `.project/stories/**` so JSON-only commits don't even attempt; AC has an explicit test that the guard is present.
- **Risk: filter rule wrong on first pass.** A SHY accidentally marked `public: true` could leak internal context to visitors. Mitigation: opt-in default (vs. opt-out) is the safer failure mode — accidentally-private is a missing-info bug, accidentally-public is a leak. Only SHY-0038 is opted-in this round; broader triage is its own SHY.
- **Risk: deterministic-sort breakage** ⇒ noisy diffs on every regen even when nothing semantic changed ⇒ commit churn. Mitigation: sort key = (phase index, priority alpha, created ISO date, SHY ID) — stable across runs. Unit test verifies determinism (run script twice, assert byte-identical output).
- **Risk: ~30s sync lag confuses contributors.** A SHY flips Done but the webpage still shows In Progress for ~30-90s. Mitigation: document the lag in CLAUDE.md's new "Public-surfacing" subsection; the absolute upper bound (CI runtime) is rare in practice (typical ~30s).
- **Risk: SHY corpus growth bloats JSON.** At 200+ SHYs, JSON could approach 500KB. Mitigation: only `public: true` SHYs are emitted; opt-in is naturally selective; if needed, future SHY adds gzipped serving via `public/_headers` `Content-Encoding`.
- **Risk: workflow needs `contents: write` permission.** Currently most workflows are read-only. Mitigation: scope the permission to just the sync workflow (`permissions: contents: write`); document in the workflow file.
- **Risk: status flip rolled in for SHY-0037 confuses reviewers.** This PR's diff includes SHY-0037's frontmatter flip + INDEX restructure (per established pattern). Mitigation: PR description enumerates the rolled-in block explicitly; commit message has a `chore(status):` co-trailer.

## Definition of Done

- [ ] `public/roadmap.html` has the new footer link with correct href + attrs + i18n.
- [ ] `scripts/sync-shy-to-roadmap-data.mjs` exists + is deterministic + has ≥30 Jest tests passing.
- [ ] `.github/workflows/sync-roadmap-data.yml` exists + actionlint clean + has the actor-check guard + path filter.
- [ ] `public/roadmap-data.json` regenerated by the script; `_meta` block present; at least one public-opted SHY appears with `shyId`/`epicId`/`status`.
- [ ] CLAUDE.md § Agile § "Public-surfacing" subsection added.
- [ ] 11 locales have both new i18n keys.
- [ ] Playwright public-pages smoke covers link + visual diff at 360/768/1280.
- [ ] End-to-end integration test passes (mutate → sync → verify; revert → sync → no-diff).
- [ ] SHY-INDEX updated: SHY-0039 row removed from Reserved (absorbed); SHY-0038 in Active during work, moves to Done in next PR per pattern.
- [ ] No regression on existing roadmap rendering (Playwright visual diff clean).
- [ ] CI green; reviewer ZERO findings (reviewer dispatched BEFORE push per [[feedback-reviewer-before-push-not-parallel]]).
- [ ] PR squash-merged.

## Notes (running log)

- 2026-06-08 ~18:15 BST — Spec authored (Option B initially: link + dead schema, speed-optimised).
- 2026-06-08 ~18:38 BST — Operator reframed criteria as quality + reliability + free-cost over speed. Re-evaluated 7 scope options (A-G with E/F/G freshly proposed). Operator selected **Option D — Full bidirectional sync** (absorbs SHY-0039); also confirmed two Day-0 design decisions: filter rule = opt-in `public: true`; loop guard = `if: github.actor != 'github-actions[bot]'`.
- 2026-06-08 ~18:42 BST — Operator confirmed canonical GH Project URL = `https://github.com/orgs/Shyden-Ltd/projects/1`. Also flagged that I need `read:project` scope on local `gh` token to verify changes; documented `gh auth refresh -s read:project` as the unblock command (operator action pending; not blocking this spec rewrite).
- 2026-06-08 ~18:45 BST — Spec rewritten end-to-end for Option D. Architect review pending (the "what's public?" filter rule + auto-commit loop guard are non-trivial design calls — architect should validate even though [[feedback-rate-limit-slowdown-strategies]] usually says skip architect for docs; this isn't a pure docs change, it's an infra design).
