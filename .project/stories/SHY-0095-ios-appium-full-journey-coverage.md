---
id: SHY-0095
status: In Review
owner: claude
created: 2026-06-13
priority: P1
effort: L
type: feature
roadmap_ids: []
epic: EPIC-0003
pr:
mvp: false
---

# SHY-0095: Extend `ios-appium-driver` to full real-iPhone native-journey coverage

## User Story

**As** the Pre-Merge gauntlet's canonical real-iPhone native cell,
**I want** `ios-appium-driver.js` to implement every method the designated native-iOS journey corpus invokes — starting with the 6 `iosShows*` presence-checks that today fall through to the fail-loud stub,
**So that** native-iOS user journeys run for real on a real iPhone via Appium + WebDriverAgent (no `return false` placeholders), giving the matrix a genuinely-operational iOS-native cell instead of a partial one.

## Why

EPIC-0003's evidence pass found `ios-appium-driver.js` (419 lines) has **5 real methods** (`iosLaunchApp`, `iosUiDump`, `iosTap`, `iosTapByTag`, `iosPersonaSignIn` + `close`) but its **6 `iosShows*` presence-checks fall through the stub-registration loop** (line 394: "any iosShows* that doesn't have a real [impl]" → `return false`). The header even claims they *"delegate to iosUiDump + iosTapByTag … regex over the XCUITest XML tree"* — aspirational, not real (the inverse-stub hazard that SHY-0092 flags and this story actually resolves by making it true). The operator chose Appium + WebDriverAgent as the canonical real-iPhone native path (2026-06-13), so this driver — not devicectl/simctl — must reach full journey coverage. `android-adb-driver.js` (2560 lines, ~79 methods) is the parity reference for what "real" looks like.

## Acceptance Criteria

### Happy path
- [ ] **Device selection (increment 1 — foundational, unblocks every AC below):** `selectUdid` returns the ECID **hardware UDID** parsed from `xcrun xctrace list devices` (e.g. `00008150-000954D90A20401C`) — the value Appium's XCUITest `appium:udid` accepts — and NEVER the CoreDevice UUID that `xcrun devicectl list devices` prints (e.g. `74563FF8-…`), which Appium rejects at session start with `Unknown device or simulator UDID`. It includes the `== Devices Offline ==` section (iOS 26/27 legacy-view), excludes the Mac host line + all simulators, and the same fixed picker is shared by the iOS web-mobile drivers. _(Unit-proven against a real captured fixture; the binding live-session proof rides the on-device gauntlet leg once the iPhone is online.)_
- [ ] Each of the 6 `iosShows*` checks (`iosShowsRoomScreen`, `iosShowsParticipantsList`, `iosShowsSeatGrid`, `iosShowsMicIcon`, `iosShowsToast`, `iosShowsRoomClosedSummary`) is a **real** implementation that reads the live `iosUiDump()` XCUITest XML and returns the correct boolean for the on-device UI — no fall-through to the `return false` stub.
- [ ] Every native-iOS journey designated for the iOS-native cell **passes on a real iPhone** via Appium against the real ShyTalk app + real backend (local stack).
- [ ] `listMethods()` reports **no journey-required method as unimplemented** — i.e. every method the designated journey corpus invokes has a real override (not the fail-loud fallback).

### Error paths
- [ ] A presence-check against a UI that genuinely **lacks** the element returns `false` because the element is truly absent in the real XML — induced by a **real seeded state** (e.g. a seeded empty participants list, a room not yet entered), NOT a mocked dump (per CLAUDE.md § No Stubs).
- [ ] A journey step that fails surfaces a real finding carrying the failing locator + an XCUITest XML excerpt (and the framework's screenshot-on-fail), not a silent false.
- [ ] If `iosUiDump()` itself fails (WDA session lost), the presence-checks surface that root error loudly rather than returning a misleading `false`.

### Edge cases
- [ ] **Enumeration (first, evidence-first):** the exact set of iOS methods the designated native-iOS journey corpus invokes is derived from the runner's iOS matchers + journey corpus and recorded; the 6 known stubs are the confirmed floor. Any method a designated journey needs **beyond** the current `IOS_METHOD_NAMES` (11) is added to the constant **and** implemented — no journey is silently skipped for a missing method.
- [ ] The header's aspirational "delegates to iosUiDump … regex over the XCUITest XML tree" comment becomes **true** (the implementation matches the claim — closing the inverse-stub hazard for this file).
- [ ] Presence-check regexes match the iOS app's **actual** XCUITest accessibility labels/types (verified against a real dump), and are resilient to benign attribute ordering (not brittle to whitespace/attribute order).
- [ ] If the enumeration reveals the surface is dramatically larger than the 11-method contract (approaching android-adb's ~79), the story **splits** with operator visibility rather than silently ballooning (recorded in Notes; per [[feedback-no-skeleton-stories-fully-refined]] a right-sized follow-up beats an unbounded one).

### Performance
- [ ] `iosUiDump()` (XCUITest source) is reused within a single matcher step where multiple presence-checks run against the same UI state (the XML source dump is the expensive call) — bounded per-cell wall-clock comparable to the Android native cell's per-journey timing.

### Security
- [ ] `iosPersonaSignIn` uses **real test personas** (`PERSONAS_PASSWORD` from `~/.shytalk/dev-personas.env`) with no credential values logged; the Appium/WDA channel is localhost-only.

### UX
- [ ] The "consumer" is a QA reader of the iOS-native cell result: a journey result is a real pass/fail with the failing element + XML context — never an empty stub-`false` that looks like a real check but proves nothing.

### i18n
- [ ] Presence-check matching is validated against the app's labels in the test locale; any reliance on a specific localized string is documented (so a locale switch does not silently break a check) — the iOS app's accessibility identifiers are preferred over visible text where available, to stay locale-stable.

### Observability
- [ ] Per-journey iOS results are recorded with the XCUITest XML excerpt on failure (the framework's existing screenshot-on-fail + per-cell log), so a reader diagnoses a failure without re-running.

## BDD Scenarios

**Scenario: a presence-check is real (true case)**
- **Given** a real iPhone in a room with a populated seat grid
- **When** `iosShowsSeatGrid()` runs
- **Then** it reads the real `iosUiDump()` XML and returns `true`
- **And** it does not fall through to the `return false` stub

**Scenario: a presence-check is real (false case, real induced absence)**
- **Given** a real seeded state where the participants list is empty
- **When** `iosShowsParticipantsList()` runs against the real UI
- **Then** it returns `false` because the element is truly absent
- **And** the `false` came from a real dump, not a mock

**Scenario: full designated journey passes on a real iPhone**
- **Given** the local stack is healthy and a real iPhone is connected (Appium auto-started per SHY-0094)
- **When** a designated native-iOS journey runs end-to-end
- **Then** every invoked iOS method has a real implementation
- **And** the journey passes against the real app + real backend

**Scenario: a journey-required method beyond the 11 is added, not skipped**
- **Given** the enumeration finds a designated journey needs a method not in `IOS_METHOD_NAMES`
- **When** the driver is extended
- **Then** that method is added to the constant and really implemented
- **And** no journey is silently skipped for a missing method

## Test Plan

Touches `ios-appium-driver.js` (+ possibly the runner's iOS matchers) → **runs the FULL Pre-Merge Testing Protocol** on a **real iPhone** (Appium + WebDriverAgent). Real app + real backend (local stack) per CLAUDE.md § No Stubs — the `false` paths are induced by real seeded UI states, never mocked dumps.

**Red → Green (framework by framework):**
- **Express/Node (Jest)** `cd express-api && npm test`:
  - **Value matrix per `iosShows*`** in `tests/scripts/drivers/ios-appium-driver.test.js`: for each of the 6 checks, a true-case (real XML fixture captured from a real device exhibiting the element) and a false-case (real XML from a seeded-absent state) → assert the **exact boolean** each way. RED before implementation (the stub returns `false` for the true-case). (Fixtures are real captured dumps, used as the deterministic input to the pure regex layer; the live-device proof is the gauntlet leg below — no synthetic/mocked UI.)
  - **Coverage assertion:** `listMethods()` ∩ {methods the designated journey corpus invokes} has **no fall-through stub** — every journey-required name has a real override (the AC-traceability "no silent skip" guard).
  - `driver-contract.test.js`, `driver-interface-pin.test.js` updated for any added `IOS_METHOD_NAMES` entries and green.
- **eslint** `npm run lint` → 0 warnings.
- **Device gauntlet (Phase 1 LOCAL):** the designated native-iOS journeys run **green on the real iPhone** via Appium (server auto-started per SHY-0094); the true/false presence behaviour is confirmed live against real seeded states; full-corpus iOS-native run at the gate.
- **Phase 2:** `code-reviewer` 100% clean → push → CI green by name (Detect Changes / Analyze JavaScript / PR Gate). The pure regex layer is CI-testable from captured fixtures; the live-iPhone journeys are local-only (no real device in CI — noted in PR).
- **Phase 3 (DEV):** re-run the iOS-native journeys on dev against the real iPhone (web = Chrome).

## Out of Scope
- `ios-devicectl` / `ios-simctl` UI inspection (non-canonical per EPIC-0003 — SHY-0092 documents them as such; they are not completed here).
- The runner's Appium server lifecycle (SHY-0094 — this story assumes a healthy server).
- Web-mobile iOS cells (Safari/WebKit) — already operational; this is the **native app** path.

## Dependencies
- **SHY-0094** (runner Appium auto-start + health-check) — this story's journeys assume a healthy Appium server.
- A **real iPhone** connected + trusted, `WDA_TEAM_ID` set + WebDriverAgent signed (SHY-0026 `setup-ios-wda.sh`); Appium + `@appium/xcuitest` installed.
- The local stack (`local/start.sh` + `npm run local`) as the real backend; real test personas (`~/.shytalk/dev-personas.env`).
- `android-adb-driver.js` as the parity reference for journey coverage shape.

## Risks & Mitigations
- **Risk:** the journey surface is far larger than 11 methods → unbounded scope. **Mitigation:** the enumeration AC bounds it up front; if it balloons toward android-adb's ~79, the story splits with operator visibility rather than silently growing.
- **Risk:** presence-check regexes are brittle to localized text or attribute order. **Mitigation:** prefer accessibility identifiers over visible text; validate against a real dump; assert order-insensitivity in the value-matrix tests.
- **Risk:** captured XML fixtures drift from the real app over time. **Mitigation:** the fixtures feed only the pure regex layer for fast RED/GREEN; the binding proof is the live-iPhone gauntlet leg, which re-validates against the real app every run.
- **Risk:** `false` cases accidentally tested via a mocked dump (No-Stubs violation). **Mitigation:** false cases use **real seeded-absent states** on the device for the gauntlet leg; fixtures are real captures, never fabricated.

## Definition of Done
- [ ] All 6 `iosShows*` checks real (live XCUITest XML); enumeration complete; every journey-required iOS method implemented (no fall-through stub for a designated journey); header comment now accurate.
- [ ] **Pre-Merge Testing Protocol satisfied** (`CLAUDE.md` § Pre-Merge Testing Protocol): per-check value-matrix Jest RED→GREEN + coverage assertion + contract/pin tests green + eslint 0 → LOCAL gauntlet runs the designated native-iOS journeys **green on the real iPhone** (true/false presence confirmed against real seeded states) → `code-reviewer` 100% clean → push → CI green by name → DEV gauntlet green on the real iPhone → **judgment-merge** (zero doubt; NO auto-merge; notify operator).
- [ ] `released_in: vX.Y.Z` set on the next release cut.

## Notes (running log)

Reviewed-up-to: 7781b025d90

- 2026-07-12 ~18:4x WIB — **Post-restart session: parallel-dispatch SECOND HALF + inc-2 + the persona-password leak all closed (TDD RED→GREEN each).** (1) `85aab5e7dce` — the inline spawnSync `dispatchOne` (blocked the event loop; runMatrix's `parallel` mode could never overlap groups) extracted to `matrix-cell-dispatch.js` on async `spawn` with the full spawnSync-era contract preserved (exit-0/1 → true/false, `CELL_TIMEOUT` same message, end-of-cell tee batching, per-cell logs); `--parallel` parses + strips from per-cell argv + previews in `--dry-run` (`parallel: true/false`) + documented in usage; `buildRunMatrixOptions` extracted for value-level CLI→runMatrix pin; `--parallel` forces capture mode sans `--report-dir` so concurrent tee blocks stay contiguous. 22 dispatcher tests vs REAL spawned fixture cells incl. a deterministic two-cell marker-file handshake that deadlocks under blocking dispatch (the revert-detector); dry-run shape pins updated. (2) `825bcf6c060` — inc-2 (R3's pre-existing Important): reserved exit code `EXIT_DRIVER_INIT_FAILED=3`; `classifyCrashExit` in matrix-dispatch classifies the single-cell `main()` catch (init → 3/`DRIVER_INIT_FAILED`, else 2/`RUNNER_CRASH`); dispatcher translates exit-3 → coded throw → runMatrix `'skip'`; capture mode flushes + writes the cell log `outcome=skip` BEFORE throwing (fulfils the matrix-cell-logs docstring promise). REAL-runner spawn tests: genuine WDA_TEAM_ID-missing init → exit 3; genuine non-init crash → exit 2. 2206/2206 across the 12 affected suites. (3) build-debug-dev.sh `set -x` leak: xtrace expanded `$PW` → REAL dev persona password in stderr + every captured build log; replaced with a redacted-echo invocation line. Two-sided unit pin (PATH-shimmed toolchain per check-node-version precedent): canary NEVER in output + canary STILL reaches xcodebuild argv (over-redaction guard). NOTE: `-allowProvisioningUpdates` deliberately KEPT for the APP build (the WDA-churn risk was the `.appium` project, now on Automatic signing with the account signed in; removing it here would break first-install device registration). Queued next: rebuild both apps (watermark fix on-device), R4 review of ALL branch commits, push→develop, relaunch dev matrix WITH `--parallel`.
- 2026-07-12 ~16:10 WIB — **Real-device bring-up surfaced 2 MORE ios-appium-driver bugs (same "Android-ism on iOS" class as selectUdid) + a hard operator-gated blocker. Built + installed the dev app; the selectUdid fix is LIVE-proven end-to-end (Appium accepted `00008150-…`, session opened).** (a) `bundleIdMap` used `com.shyden.shytalk.dev/.local` but iOS ships a SINGLE `PRODUCT_BUNDLE_IDENTIFIER` `com.shyden.shytalk` for every config (pbxproj-confirmed; installed Debug-Dev app IS `com.shyden.shytalk`) — the config picks the backend via AppEnvironment, not a suffixed id. Fixed (all targets → the single id + `IOS_BUNDLE_ID` override); the false local/dev tests that pinned the suffixes corrected (they were green over the bug). (b) `xcodeSigningId: 'Apple Developer'` matches NO certificate — real cert is `'Apple Development'`; confirmed by the live WDA rebuild failure (`xcodebuild code 65: "No certificate ... matching 'Apple Developer' found"`). Fixed + session-caps test pins it. TDD RED→GREEN both; `ios-appium-driver.test.js` **36/36** · eslint 0 · prettier. Commit `f0f9720ef56` (post-R3 → needs an R4 pass on these 2 before merge). **HARD BLOCKER (operator-gated — DEFINITIVE root cause diagnosed over ~5 escalating WDA errors): Xcode has NO Apple Developer account signed in.** The `Apple Development: Sean Steberis (2YV2Y2GFD3)` cert IS in the keychain, but WDA can't be signed: the old setup used MANUAL signing with a `WDA Dev Wildcard 2026-06-30` profile (gone — churned by `build-debug-dev.sh`'s `-allowProvisioningUpdates`, or expired); switching WDA to AUTOMATIC signing (operator-approved reset — `CODE_SIGN_STYLE=Automatic` in `~/.appium/.../appium-webdriveragent/WebDriverAgent.xcodeproj`, backed up `.bak-claude-*`) then failed because automatic signing needs a signed-in account to MINT the profile — direct `xcodebuild build-for-testing -allowProvisioningUpdates` returns `error: No Accounts: Add a new account in Accounts settings` + `No profiles for 'com.shyden.WebDriverAgentRunner.xctrunner'`. The earlier "Developer App Certificate is not trusted" messages were downstream symptoms of the unsigned WDA, NOT a device-trust gap (operator confirmed VPN & Device Management is empty). **Operator action to unblock:** Xcode-beta → Settings → Accounts → sign in with the Apple Developer account → then the automatic reset mints the WDA profile with the existing cert → I retry (`IOS_FORCE_NEW_WDA=true` forces the fresh WDA build). GUI-only; can't be automated. **Driver change this round:** made `useNewWDA` env-configurable (`IOS_FORCE_NEW_WDA=true` forces a fresh WDA build+install after a signing change — else Appium launches a stale/unsigned WDA → "connection refused to port 8100"). Commit pending. **Once WDA signs, the live journey run proves all 3 driver fixes end-to-end + yields [[SHY-0151]]'s device-enforcement evidence.** Queued: R4 review of the bundleId/cert/useNewWDA commits; inc-2 (subprocess skip-classification, task #14); the persona-password `set -x` leak in build-debug-dev.sh; the WDA `.appium` project change is outside the repo (env setup, not committed).
- 2026-07-12 ~15:30 WIB — **R3 re-review of `7781b025d90` → DELTA 100% CLEAN; R2 Critical fully resolved; status → In Review to merge increment 1.** R3 confirmed (with its own repo-wide grep + hand-trace): the `isInitError` code-first classification is correct for BOTH consumers at the function level; each driver's `DRIVER_INIT_FAILED` code assertion is non-tautological (`Object.assign(new Error, {code})` preserves `instanceof Error`/stack; reverting to bare `new Error` → `code===undefined` fails the test); no `err.code` collision across the tree (all other codes — `CELL_TIMEOUT`, `ETIMEDOUT`, `STEP_NOT_IMPLEMENTED`, `auth/*`, `MODULE_NOT_FOUND`, `ENOENT` — are distinct); zero remaining `no connected iPhone` hits outside this story's own Notes. **R3 surfaced ONE new Important that is PRE-EXISTING (not from this delta, confirmed): the real `--matrix` CLI path dispatches each cell as a `spawnSync` subprocess and `manual-qa-runner.js:16195` does `return proc.status === 0`, collapsing exit-2 (driver-init crash / no device) and exit-1 (real assertion failure) both to `'fail'` — so the `DRIVER_INIT_FAILED`/`isInitError` skip path is reached by `runHealthCheck` (`--check-drivers`) but NOT by a real `--matrix` run for ANY driver's no-device condition.** This gap predates R1 (nothing in the selectUdid chain touched the subprocess dispatch) and does not corrupt a device-present journey run (real product-bug failures still classify as `'fail'` correctly). Tracked as a follow-up increment (inc-2) THIS session per [[feedback-fix-pre-existing-and-new-same]]: have the single-cell process signal init-failure via a reserved exit code and `dispatchOne` re-throw a `DRIVER_INIT_FAILED` Error so the skip-not-fail behaviour reaches real `--matrix` runs. **Increment 1 (this merge) = the selectUdid xctrace hardware-UDID fix + the isInitError code-hardening**; iosShows* real impls, method enumeration, and the subprocess-dispatch inc-2 remain. Full loop: R1 (4 Imp) → R2 (1 Crit + 1 Imp) → R3 (delta clean). 5 affected suites 241/241 · eslint 0 · prettier clean · live-on-device proven.
- 2026-07-12 ~15:05 WIB — **R2 re-review of `3433823766b` found 1 Critical (a regression R1's OWN fix introduced) + 1 Important + minor gaps → ALL resolved in `7781b025d90`.** **Critical:** renaming the iOS "no device" error `no connected iPhone found` → `no physical iPhone found` (R1 finding 2) broke `matrix-dispatch.js::isInitError`, whose ONLY iOS signature was `/no connected iPhone found/i` — so an iOS cell with no iPhone would report **FAIL instead of SKIP**, and under `--fail-fast` could abort the ENTIRE matrix (Android + web too). `driver-health-check.js` reuses the same classifier → same break. **Five stale synthetic fixtures kept both suites green over the regression** (the [[feedback-mutation-passed-means-investigate]] "passing mutation" pattern — the source string changed, the hand-typed fixtures didn't). **Root-cause fix (structural, per [[feedback-root-cause-not-symptom]] — not another string tweak):** the 3 iOS driver factories now set `err.code = 'DRIVER_INIT_FAILED'` on their no-device throw (the code `isInitError` checks FIRST), so classification no longer depends on message wording; each driver's own test now pins that code so it can't silently drift again. The regex signature is updated to the real text as defense-in-depth, the 5 fixtures corrected, and (Important) the two operator troubleshooting docs (`QA_FRAMEWORK_SETUP.md`, `QA_FRAMEWORK_CELL_RUNBOOK_IOS.md`) now show the real symptom + the "Devices Offline often still usable" remediation. Minor: pinned `timeout: 15000` in the xctrace-invocation test. **R2's regex conclusions were hand-traced (no shell tool); I had independently `node -e`-verified all 7 adversarial cases in R1.** Verified post-fix: 5 affected suites **241/241** · eslint 0 · prettier clean. R3 re-review of the delta next.
- 2026-07-12 ~14:35 WIB — **R1 code-review of `4427e8e86ae` (0 Critical / 4 Important / 6 named coverage-gap edge cases) → ALL resolved in `3433823766b`.** Findings + fixes: (1) stale `xcrun devicectl` reference in the ios-appium-driver.js file header → xctrace; (2) the sibling web-mobile Safari/WebKit drivers still threw `no connected iPhone found via \`xcrun devicectl list devices\`` (and a test pinned EACH — `web-mobile-safari-ios-driver.test.js:94`, `web-mobile-webkit-ios-driver.test.js:195`) — they share the fixed `selectUdid`, so the messages now lied; updated both error strings + the safari docstring + both pinned test regexes to xctrace; (3) `selectUdid`'s `xctrace` exec had no timeout → could wedge the runner on a stale usbmuxd tunnel; added `timeout: 15000` (kill → catch → null → fail fast); (4) `selectUdid`'s catch silently swallowed errors (inconsistent with the file's other catches) → now logs a diagnostic that distinguishes "xctrace errored" from "no device". Coverage gaps closed, **each verified against the live regex via `node -e` before writing the test** (per [[feedback-reviewer-regex-anchoring-false-alarms]]): embedded decoy `(N) (hex)` pair in a device name (last-pair-wins via the `$` anchor), two real devices → first-in-order, trailing whitespace after the udid paren, and blank/empty/padded preferred-udid (blank/whitespace now falls through to auto-detect; padded is trimmed — new guard). The reviewer noted it had no shell tool so its regex conclusions were hand-traced; I re-verified all 7 adversarial cases with the live regex (7/7 as it traced). Verified post-fix: 3 iOS driver suites **98/98** · eslint 0 · prettier clean. R2 re-review next.
- 2026-07-12 ~14:10 WIB — **Picked up (Draft → In Progress) + increment 1: `selectUdid` device-selection fix.** Surfaced while confirming whether the develop iOS journey cell actually drives the real iPhone (operator asked "can you confirm you're testing both devices?"). **It was not, and never had been.** Evidence (real, from the 2026-07-11 `/run-journeys` local matrix, `/tmp/run-journeys-20260711-235929-local/log`): every iOS cell died with `Appium /session failed (500) … "Unknown device or simulator UDID: '74563FF8-D1FC-567D-A6C1-7C8C3CEFE0C6'"`. **Root cause:** `ios-appium-driver.js::selectUdid` parsed `xcrun devicectl list devices`, whose Identifier column is the **CoreDevice UUID** (`74563FF8-…`). Appium's XCUITest driver (3.4.2 / xcuitest 11.11.2) rejects that — `appium:udid` needs the ECID **hardware UDID** (`00008150-000954D90A20401C`), which only `xcrun xctrace list devices` prints. A prior "make both drivers select the same device" unification (docstring line 77) had wrongly collapsed the Appium picker onto the devicectl driver's — but the two legitimately need **different** identifiers (Appium → hardware UDID; devicectl → CoreDevice UUID for its app-lifecycle commands). **This is exactly [[feedback-mutation-passed-means-investigate]]:** the committed `ios-appium-driver.test.js` had a *green* test pinning `74563FF8-…` as the correct `appium:udid` — a false test encoding the bug. **Fix (TDD, RED watched against the real fixture first):** `selectUdid` now runs `xctrace list devices`, splits off the `== Simulators ==` section (else a no-real-device run returns a simulator UDID → false green + real-device-only violation), and matches the `(osver) (udid)` double-paren shape (excludes the Mac host, which has a UUID but no os-version paren). It **keeps** `== Devices Offline ==` — on iOS 26/27 a wired, drivable iPhone routinely shows there in xctrace's legacy view (excluding it returned null + killed the whole iOS matrix, 2026-07-02). Rewrote the false test into a value-matrix (online/offline/mac-excluded/sim-excluded/xctrace-invoked/preferred-passthrough/throws-null) + a **regression guard** that feeds devicectl output and asserts the CoreDevice UUID is never returned (goes RED on a revert-to-devicectl). **Positive ripple:** `web-mobile-safari-ios-driver.js` + `web-mobile-webkit-ios-driver.js` both `require('ios-appium-driver')`'s `selectUdid`, so the 4 iOS web-mobile browser cells were **equally broken** and are fixed by the same change — correcting this story's "web-mobile iOS cells already operational" Out-of-Scope claim (they weren't; device selection was shared-broken). **Verified (unit):** `ios-appium-driver.test.js` 29/29 · driver-contract/interface-pin/loader 112/112 · web-mobile-safari+webkit 63/63 · eslint 0 · prettier clean. **Verified (LIVE, 2026-07-12):** ran the fixed driver against the real iPhone — `selectUdid()` returned `00008150-000954D90A20401C` (the hardware UDID) from the real `xctrace` output *with the device sitting under `== Devices Offline ==`*, and Appium **ACCEPTED** it (`iosLaunchApp` got past session bootstrap — WDA launched on-device; the only remaining error is `App with bundle identifier 'com.shyden.shytalk.local' unknown`, i.e. the app build isn't installed yet, NOT a UDID/device error). This proves (a) the fix works end-to-end on real hardware, and (b) the "iPhone Offline in xctrace" state was a **cosmetic legacy-view quirk for a CoreDevice-era device, never a reachability blocker** — usbmuxd reports the hardware UDID over USB and `devicectl` holds a live tunnel (`fd1c:a690:…`), so **no reboot was needed** (the historical "reboot to fix xctrace-offline" runbook did not apply here). Remaining to run full native journeys: install a ShyTalk build on the device (com.shyden.shytalk.local/.dev). `iosShows*` real impls + method enumeration remain future increments; story stays In Progress.
- 2026-06-13 — Filed under EPIC-0003 (child build-order item **C**, the substantive one). Evidence at filing: `IOS_METHOD_NAMES` has 11 names; real overrides exist for `iosLaunchApp`/`iosUiDump`/`iosTap`/`iosTapByTag`/`iosPersonaSignIn`/`close` (lines 203/231/245/278/322/406); the 6 `iosShows*` fall through the stub loop (line 394) → `return false`; header lines 60–64 claim a delegation that doesn't exist yet. android-adb (2560 lines, ~79 methods) is the parity reference. The exact "full coverage" method set is **evidence-derived in the enumeration AC**, not guessed — the 6 stubs are the confirmed floor.
