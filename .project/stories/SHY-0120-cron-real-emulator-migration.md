---
id: SHY-0120
status: In Progress
owner: claude
created: 2026-06-17
priority: P1
effort: L
type: refactor
roadmap_ids: []
epic: EPIC-0003
pr:
mvp: false
---

# SHY-0120: Migrate the remaining cron tests to the real Firestore emulator (EPIC-0003)

## User Story

**As** the team executing EPIC-0003,
**I want** the remaining cron tests (~10 beyond SHY-0110's backpackCleanup — closedRooms / archiveReports / subscriptions / … ) moved off `jest.mock` + `makeStatefulFakeDb` onto the real Firestore emulator (provisioned in CI by SHY-0109),
**So that** every scheduled job is verified by its real outcome (the right docs actually deleted/archived/updated) instead of hollow `toHaveBeenCalled` mock-call assertions.

## Why

Crons act unattended on production data, so a cron that deletes/archives the wrong documents is high-impact — and the current `toHaveBeenCalled`-style mock tests would not catch it ([[feedback-ac-traceability-in-tests]]). SHY-0109 shipped the emulator-in-CI keystone + `tests/helpers/firebase-emulator.js`, and SHY-0110 migrated the first cron (backpackCleanup), establishing the `collectionGroup` + de-mock-logger patterns. This area drains the remaining ~10 crons using those proven patterns. Some crons depend on r2/fcm/external services and need their own real-path approach (called out per-cron at pickup).

## Acceptance Criteria

### Happy path
- [ ] Every remaining cron test (closedRooms / archiveReports / subscriptions / expiry / cleanup jobs) runs against the real Firestore emulator; no `jest.mock`/`makeStatefulFakeDb` remains in them; each sets `NODE_ENV=local` + uses `assertEmulatorReachable()` + the emulator helpers.
- [ ] Each migrated cron seeds real state (e.g. real closed rooms / real expired reports), runs the real job, and asserts via real reads that the **correct** docs were mutated and the others retained (value-level, not "the mock was called").

### Error paths
- [ ] Each cron's empty-result branch is exercised against a genuinely empty real collection (clean slate) and asserted to be a no-op.
- [ ] No assertion depends on the logger (real `log` runs unmocked; banned `expect(log.x).toHaveBeenCalled()` shape removed).
- [ ] A cron with an r2/fcm/external dependency exercises the **real** sandbox path for that dependency (or escalates via the operator escape-hatch if genuinely un-inducible — never a silent mock).

### Edge cases
- [ ] Each cron's threshold/boundary (e.g. `expiresAt <= now`, `closedAt older than N`) is exercised at the real value level — boundary item mutated, just-past-boundary item retained.
- [ ] `collectionGroup` correctness where applicable: items under different parents all collected; unrelated same-named structures not falsely matched (the SHY-0110 pattern).
- [ ] Surfaced bugs: non-blocking → `type: bug` SHY + `@known-failure-SHY-NNNN` (assertion intact); blocking → pivot-fix TDD-first.

### Performance
- [ ] Real-state setup/teardown batched; migrated cron suites complete in a few seconds against a warm emulator; no per-doc round-trip storm.

### Security
- [ ] Emulator sandbox (`demo-shytalk`); no secrets logged; r2/fcm sandbox keys only for the external-dep crons.

### UX
- [ ] N/A — backend scheduled jobs; no user-facing surface.

### i18n
- [ ] N/A — no user-facing strings (cron-internal).

### Observability
- [ ] Each cron's real `log.info('cron', …)` runs unmocked during tests (exercised, proving the logging path does not throw against the real emulator).

## BDD Scenarios

**Scenario: closedRooms archives the right rooms by real outcome**
- **Given** real rooms — some closed past the threshold, some recent
- **When** the real closedRooms cron runs
- **Then** real reads show the past-threshold rooms archived/cleaned and the recent ones retained

**Scenario: a cron's empty collection is a no-op**
- **Given** the cron's target collection emptied for real
- **When** the cron runs
- **Then** it resolves without error and mutates nothing

**Scenario: an external-dep cron uses the real sandbox path**
- **Given** a cron that calls r2/fcm
- **When** it runs in the test
- **Then** it exercises the real sandbox path (not a mocked client), asserting the real Firestore outcome

**Scenario: a surfaced cron bug is catalogued**
- **Given** a migrated real test exposes a non-blocking defect
- **When** triaged
- **Then** a `type: bug` SHY is filed + the test tagged `@known-failure-SHY-NNNN` with its correct assertion intact

## Test Plan

**RED:** rewrite each cron test to require the real emulator (no mocks) → fails until seeded real; run without an emulator → fails fast via `assertEmulatorReachable()`.

**GREEN:** per cron — seed real state → run real job → assert real post-state + boundary + empty branch; de-mock the logger; for external-dep crons wire the real sandbox path; file + `@known-failure`-tag surfaced bugs. Bring up local stack → canonical `npm test` green.

**Frameworks:** express Jest (real Firestore emulator + r2/fcm sandbox where needed), frontmatter validator. **Real backend:** Firestore emulator (`demo-shytalk`) + r2/fcm sandbox for external-dep crons. **Gauntlet exemption:** backend cron harness — no app/web/device surface; authoritative proof = CI-green (Test Backend exercises the emulator).

## Out of Scope
- backpackCleanup (already migrated by SHY-0110).
- Per-jest-worker emulator isolation (SHY-0109 scaling item) unless a cron's global collection-group state forces it.
- Sub-splitting: ~10 crons delivered as 1-SHY-1-PR slices (or small grouped PRs by dependency profile) at pickup.

## Dependencies
- **SHY-0112** (keystone) first.
- **SHY-0109** (emulator-in-CI) + **SHY-0110** (first cron migration, established patterns) + `tests/helpers/firebase-emulator.js`.
- Local emulator stack; r2/fcm sandbox for external-dep crons.

## Risks & Mitigations
- **Risk:** external-dep crons (r2/fcm) are harder to make real. **Mitigation:** real sandbox path; operator escape-hatch escalation if a specific condition is genuinely un-inducible, never a silent mock.
- **Risk:** `collectionGroup` global-state collisions between cron tests. **Mitigation:** per-group clean-slate (`clearCollectionGroup`) in `beforeEach`; documented per-worker namespacing as the scaling answer.

## Definition of Done
- All remaining cron tests double-free + asserting real value-level outcomes; baseline shrinks per file.
- Surfaced bugs filed + `@known-failure`-tagged with intact assertions.
- Canonical `npm test` green; `code-reviewer` zero findings; CI green by name incl. Test Backend.
- Judgment-merge per slice. Each slice → In Review → Done on its release cut.

## Notes (running log)
- **2026-06-17 — created Draft (P1, ~10 crons, L).** Continues SHY-0109/0110; reuses the `collectionGroup` + de-mock-logger patterns. External-dep crons (r2/fcm) get the real sandbox path, not a mock; escape-hatch if genuinely un-inducible.
- **2026-06-18 ~02:55 BST — picked up (→ In Progress); pickup-fitness mapped the real landscape.** Actual remaining mock-using cron tests in `express-api/tests/cron/` = **13** (backpackCleanup already 0; expireTempIds=1 near-done): Firestore-only — `testDataCleanup`(82), `ageVerificationAuditReconcile`(46), `closedRooms`(36), `index`(32), `serverHealth`(28), `subscriptions`(27); external-dep (R2/FCM/email, need real-sandbox path) — `accountDeletion`(158), `archiveReports`(59), `orphanedStorage`(53), `backups`(44), `rotateLogs`(41), `expireDataExports`(21). The story's named examples (closedRooms/archiveReports/subscriptions) all still exist — count estimate accurate; sequencing is by dependency profile (Firestore-only first, external-dep crons each get the real sandbox path as separate slices). Picked up while SHY-0102 is parked at its operator device-gate (operator: "work on something else while you're waiting").
- **2026-06-18 — SLICE 1: `testDataCleanup` (Firestore-only, 82 mock-lines → real emulator).** Chosen first: largest Firestore-only cron, zero external deps (no R2/FCM/email) → fully ungated + self-contained. The old 18 tests were mostly hollow (`db.collection toHaveBeenCalledWith`, `ref.delete toHaveBeenCalled`) — could not catch the cron deleting the WRONG docs. Rewriting each as a real value-level assertion (correct docs deleted, siblings retained) against the live Firestore emulator; `_testRun`-prefix scoping + `createdAt`-cutoff boundary + user/conversation subcollection deletion + linked deviceBans/networkBans (numeric+string uniqueId variants) + starting-screen key-prefix deletion + counter restore + production guard, all proven by real reads. Logger de-mocked (runs unmocked, not asserted).
- **2026-06-18 — SLICE 1 DONE (verified).** 19 real tests, **19/19 pass in ~2.4s** against the live Firestore emulator; **RED-sensitivity proven** (temporarily zeroed the cron's `staleDocs` → the sweep test failed; restored → green). eslint/prettier clean; `check-no-new-stubs` baseline tightened (`testDataCleanup` removed → jest.mock 194→193, jest.fn 226→225, mockResolvedValue 202→201). Even the `cleanupTestStartingScreens` best-effort catch is exercised by a REAL induced error (an invalid `..` field-path in `update()`), not a mock. On branch `story/SHY-0120-cron-real-emulator-migration` off origin/main. **Gotcha (re-learned):** emulator-backed express tests MUST run via the canonical runner `node --experimental-vm-modules node_modules/.bin/jest` (i.e. `npm test`) — bare `npx jest` cannot load the Firebase Admin SDK's transitive **ESM `uuid@14`** and fails with `Unexpected token 'export'` (a false red that wasted a cycle; the `--experimental-vm-modules` flag is the fix). **Remaining slices (next pickups):** Firestore-only — `ageVerificationAuditReconcile`, `index`, `serverHealth`; external-dep (real R2/FCM/email sandbox path) — `accountDeletion`, `archiveReports`, `orphanedStorage`, `backups`, `rotateLogs`, `expireDataExports`.
- **2026-06-18 — SLICE 3 DONE (verified): `closedRooms` (delete >7-day CLOSED rooms + subcollections, 36 mock-lines → 9 real tests).** Seeds real rooms → runs real cron → reads back: a >7-day CLOSED room + its `messages`/`seatRequests` subcollection docs deleted; recent (`closedAt` <7d), missing-`closedAt`, and non-CLOSED (state scoping) rooms retained; the do/while message pagination exercised by a batch-seeded **exactly-500-message** room (cheap single-batch setup forces the loop to run twice); the 20-per-run cap proven by seeding 21 old CLOSED rooms → exactly 1 survives; multi-room loop continuation. **9/9 green ~1.4s; RED-sensitivity proven** (zeroed the cron's `old` list → deletion test failed; restored). The per-room try/catch is a defensive guard (a real Firestore delete failure is not cheaply inducible — loop continuation is proven by the multi-room test instead). eslint/prettier clean; ratchet tightened (closedRooms removed → jest.mock 192→191, jest.fn 224→223, mockResolvedValue 200→199).
- **2026-06-18 — SLICE 4 DONE (verified): `ageVerificationAuditReconcile` (compliance audit-log back-fill, mock query-shape fakes + pinned `now` → 18 real value-level tests + pure toMillis/exports).** Seeds real `ageVerificationSubmissions` + `auditLog` → runs the real cron → reads `auditLog` back. Covers: full remediation-row value assertion (action/actionType/targetType/targetId/adminUid/fromSubmissionId/method/originalDecisionAt/note/timestamp); the status→action **value matrix** (all 5 STATUS_ACTION_MAP keys: approved/rejected/dob_modified/modify-dob/modifyDob → exact actions); method-leak scoping (rejected omits the approve-only method); DOB-delta caveat note; adminUid fallback to 0 (missing + non-numeric decidedBy); tagged-row idempotency; **end-to-end run-twice → no duplicate** (the strongest real proof — second run finds the row it wrote); original-write window match in/out of ±10 min; targetId scoping; **REAL Firestore Timestamp coercion** (seeded a JS Date → read back a genuine Timestamp, exercising toMillis path the mock faked); skippedPending (via numeric in-window `status:'pending'`); skippedUnknownStatus; the 7-day range-query boundary (8-day-old excluded); multi-doc loop continuation. **2 real-vs-mock corrections surfaced + encoded** (verified against the emulator with a probe): (1) the numeric `where('decisionAt','>=',cutoff)` range query is **type-aware** — a string/out-of-window decisionAt is excluded by the QUERY and never scanned, so the mock's "string → skippedPending" path is unreachable in prod (now asserted as not-scanned); (2) the per-doc `failed` counter + `data()`-throws branch is a defensive guard not cheaply real-inducible (every seedable value is Firestore-storable → the rebuilt entry is storable → add() won't throw) — loop continuation proven by the multi-doc test, a genuine outage is operator escape-hatch territory. **26/26 green ~0.96s; RED-sensitivity proven** (skipped the cron's real `auditLog.add` → 11 read-back tests failed; restored). eslint/prettier clean; ratchet tightened (ageVerificationAuditReconcile removed → jest.mock 191→190, jest.fn 223→222, mockResolvedValue 199→198). Commit `e211b198353`. **Classification note for the next slices:** `index`(32) is the cron **registry** (pure `node-cron` scheduling + prod-guard, no Firestore/Auth/network collaborator) → genuine UNIT test; correct EPIC-0003 move is RELOCATE/RENAME to a `*.unit.test.js` location (mocking `node-cron` is legit unit isolation), NOT rewrite-to-real. `serverHealth`(28) is MIXED — real in-process memory + `alertManager.createAlert`(real→Firestore) + `pm2 jlist` via execFile (external binary, escape-hatch territory).
- **2026-06-18 — SLICE 5 DONE (verified): `index` (cron registry, 32 mock-lines) — RECLASSIFIED to a unit test, NOT rewritten.** Investigation confirmed `src/cron/index.js` is the scheduling REGISTRY: `startCronJobs()` reads NODE_ENV (prod-guard) + calls `node-cron.schedule(expr, callback)` to wire each job. It exercises NO real collaborator — `node-cron` is an in-process scheduler (cannot "really" wait until 03:00 UTC for a job to fire), each job module has its OWN real-emulator test, and alertManager's real behaviour belongs in alertManager's own test. The only thing under test is the WIRING (7 prod schedules + exact cron expressions, dev prod-guard, callback delegation, error-`.catch` paths, age-verif catastrophic-alert path). There is no "real" version of a scheduler-wiring test, so the correct EPIC-0003 move (explicitly sanctioned by `check-no-new-stubs.js`'s own error message: "move it to a unit-test location if it is genuinely a unit test") is `git mv tests/cron/index.test.js → index.unit.test.js` + a classification docstring — mocking node-cron + the job modules is correct unit isolation, not migration debt. 17/17 still green via the canonical runner (no logic change); eslint/prettier clean. Ratchet: `isUnitTestLocation` now exempts the `*.unit.test.js` file → it drops from the integration debt (jest.mock 190→189, jest.fn 222→221, mockResolvedValue 197). **Open for reviewer/operator veto** if they prefer a different classification — but rewrite-to-real is impossible for a scheduler. Remaining: `serverHealth`(28, MIXED — real memory + alertManager→Firestore + pm2 external) + the external-dep crons (accountDeletion/archiveReports/orphanedStorage/backups/rotateLogs/expireDataExports — real R2/FCM/email sandbox path).
- **2026-06-18 — SLICE 6 DONE (verified): `serverHealth` (MIXED cron, 56 mock-occurrences → real integration + pure-logic unit; OPERATOR-DECIDED Option A).** serverHealth has real-inducible paths (memory-threshold alert, real execFile boundary, in-flight guard) AND an un-inducible one (PM2 restart-DELTA detection needs a real pm2 daemon with restartable procs — no CI runner has it). Surfaced this as a precedent-setting EPIC-0003 escape-hatch decision (AskUserQuestion); **operator chose Option A — extract a pure helper.** Done: (1) behaviour-preserving refactor of `serverHealth.js` — extracted `detectPm2Restarts(processes, lastCounts)` (faithful: preserves the `!pm2_env` skip, the `lastKnown > 0` first-sighting-baseline guard, the delta math, the in-place mutation), exported for unit access; (2) `serverHealth.unit.test.js` (NEW, unit location) — 10 PURE tests of `detectPm2Restarts` with REAL data arrays, ZERO doubles (increase-from-baseline, first-sighting-no-alert, unchanged, DECREASED-no-alert, multi-process independence, multi-restart, no-pm2_env skip+not-recorded, restart_time 0, missing restart_time, empty list); (3) `serverHealth.test.js` REWRITTEN as REAL integration (4 tests) against the live emulator + real emulator-backed alertManager — no memory faking (steered by THRESHOLD: tiny 0.01% fires on real RSS, 99.9% doesn't; note `serverMemoryWarningPercent || 30` means a seeded 0 falls through, hence 0.01), real `alerts` doc value-level assertion (severity/title/status/context.rssMB/systemTotalMB/rssPercent), in-flight guard proven by concurrent double-call → exactly ONE alert, real `execFile('pm2','jlist')` runs gracefully (absent→err branch / present→no first-run delta). Config steering via real `applyConfig` (seed `alertConfig/settings` → `_clearState` → real `trackSlowEndpoint(_,0)` warms the sync getConfig cache without writing an alert). **14/14 green (10 unit + 4 integration) ~1s; RED-sensitivity proven on BOTH surfaces** (probe 1: unreachable mem threshold → 2 integration tests failed; probe 2: `lastKnown >= 0` → the first-sighting unit test failed; both restored). eslint clean (renamed param to avoid `no-shadow` of the module-level `lastRestartCounts` — warnings-are-failures), prettier clean. Ratchet tightened (serverHealth.test.js removed → jest.mock 189→188, jest.fn 221→220, mockResolvedValue 197→196; serverHealth.unit.test.js exempt as a unit location). **Remaining: external-dep crons** (real R2→MinIO / email→Mailpit / FCM sandbox) — `accountDeletion`(158), `archiveReports`(59), `orphanedStorage`(53), `backups`(44), `rotateLogs`(41), `expireDataExports`(21). These need the FULL local stack up; FCM-delivery assertions may hit the same un-inducible-external decision (Option A precedent applies — extract/assert the real Firestore outcome, treat true external delivery as escape-hatch).
- **2026-06-18 — SLICE 7 DONE (verified): `expireDataExports` (first external-dep cron — real Firestore emulator + real MinIO/R2).** First slice to drive the real R2 storage layer, not just Firestore. Migrated off the `firebase`+`r2`+`log` Jest mocks → 7 real tests against the live stack: users seeded with `db.doc().set()`, export objects PUT for real via `r2.putObject` (NODE_ENV=local → MinIO http://localhost:9002, bucket `shytalk-media`, creds minioadmin — all `r2.js` defaults, verified to match docker-compose), deletion verified by **listing the bucket** (not a spy on `deleteObjects`). Covers: real delete + doc flip to `expired`/null-key when expiry passed; not-yet-expired left untouched (object + doc); **both query clauses proven by real excluded docs** — `dataExportExpiresAt > 0` (a `expiresAt:0` ready doc survives) and `dataExportStatus == 'ready'` (a `generating` past-expiry doc survives); ready+expired with NO R2 key flips without an R2 call; mixed-batch run (expired-with-key deleted, expired-no-key flipped, future preserved — loop continuation + isolation); **`.limit(CRON_LIMIT)` cap proven by 501 expired docs → exactly 500 flip + 1 backlog remains `ready`** (batched seed; the fire-and-forget `log.warn('hit CRON_LIMIT')` is the observability echo — `log.js` swallows its async write, so the cap is verified by the surviving backlog, not by matching the log line). **Per-doc `catch` branch = documented Option-A escape-hatch** (S3/MinIO `DeleteObjects` is idempotent, root creds never deny, emulator accepts every write → a single doc's deletion can't be made to throw without a mock; no pure logic to extract → loop robustness proven by the multi-doc run). **7/7 green ~1.9s; RED-sensitivity proven twice** (probe 1: skipped `r2.deleteObjects` → the 2 object-deletion tests failed; probe 2: removed the in-loop `expiresAt > now` guard → the 2 future-skip tests failed; both restored, prod cron byte-clean vs HEAD). eslint/prettier clean; ratchet tightened (expireDataExports.test.js removed → jest.mock 188→187, jest.fn 220→219, mockResolvedValue 196→195). Commit `527f55974b5`. **Remaining external-dep crons:** `accountDeletion`(158), `archiveReports`(59), `orphanedStorage`(53), `backups`(44), `rotateLogs`(41).
- **2026-06-18 — SLICE 8 DONE (verified): `archiveReports` (resolved-report archival — real Firestore emulator; NO external dep after all).** Source inspection corrected the handoff's "external-dep" label — this cron is Firestore-only (`reports` → `reportsArchive`, no R2/email). Old test was pure structural mocking (`expect(setCalls[0][0]._path).toBe('reportsArchive/report-1')`, `whereArgs` introspection) — asserted HOW the SDK was called, not that a report moved. Migrated → 6 real tests: archives a resolved >6mo report (value-level — all fields + the injected `id` from `{id:d.id,...d.data()}` read back) AND deletes the original; both query clauses proven by real excluded docs (old `pending` stays; recent-resolved stays); mixed run = selective archival + isolation; empty-match no-op (snapshot.empty return); **>250 chunk boundary proven by 260 real eligible docs → all 260 archived + reports emptied** (no boundary drop). Dropped the 2 contrived `propagates …errors` tests (escape-hatch — no real emulator read/commit failure inducible without a mock; cron has no catch so propagation is structural). **6/6 green ~1.3s; RED-sensitivity proven** (skipped `batch.set` archive copy → the 3 archive-content tests failed; restored, prod byte-clean). Caught + fixed a TEST bug (not product): `monthsAgo(7)` evaluated twice drifted on `Date.now()` → pinned once. eslint/prettier clean; ratchet tightened (archiveReports.test.js removed → jest.mock 187→186, jest.fn 219→218, mockResolvedValue 195→194). Commit `8403e1d1463`. **Remaining crons:** `orphanedStorage`(169-src), `backups`(228-src), `rotateLogs`(103-src), `accountDeletion`(594-src) — these DO touch R2/email/FCM; verify per-cron at read time.
- **2026-06-18 — SLICE 9 DONE (verified): `rotateLogs` (log-rotation to R2 — real Firestore emulator + real MinIO; most collaborator-rich cron yet).** Touches Firestore config (`logConfig/settings`) + Firestore `logs` query/delete + R2 NDJSON write + R2 list/prune. Old test faked all three + spied on `Date.now()`; asserted "putObject was called". Migrated → 5 real tests proving the actual log-rotation **contract a mock cannot**: the rows deleted from Firestore are **READ BACK from the R2 archive as valid NDJSON** (parsed line-by-line, injected `id` confirmed). Covers: archive+delete+NDJSON-recoverability (value-level); custom `retentionHours` from a real config doc steers the cutoff (old archived, fresh retained); default-48h window archives nothing when all logs fresh (also exercises config-absent→default path); **pruneOldLogs** proven by real dated R2 keys — `logs/2020/...` pruned (>90d), today's kept (<90d), non-dated `keepme.txt` ignored by the regex guard (no `Date.now()` spying needed — a 2020 key is unconditionally >90d); **`.limit(500)` cap** proven by 501 expired logs → 500 archived (NDJSON has 500 lines) + 1 backlog. Config-read `catch` (un-inducible emulator get-failure) + fire-and-forget truncation warn = documented escape-hatch. **5/5 green ~0.9s; RED-sensitivity proven twice** (probe 1: skipped `r2.putObject` archive → 2 archive tests failed; probe 2: skipped `r2.deleteObjects` prune → prune test failed; both restored, prod byte-clean). Read R2 objects via `resp.Body.transformToString()` (real AWS SDK v3 stream). eslint/prettier clean; ratchet tightened (rotateLogs.test.js removed → jest.mock 186→185, jest.fn 218→217, mockResolvedValue 194→193). Commit `3d0421fc778`. **Remaining crons (3 biggest):** `orphanedStorage`(169-src), `backups`(228-src), `accountDeletion`(594-src).
- **2026-06-18 — SLICE 10 DONE (verified): `orphanedStorage` (R2 garbage-collector — real Firestore emulator + real MinIO).** Replaced 29 mock tests that only asserted `deleteObjects` call SHAPES (`.not.toHaveBeenCalledWith([refKey])`) — never proved a referenced object survived on disk. Migrated → 7 real tests: referencing docs seeded, real objects PUT per folder, object existence read back from the bucket (referenced kept / orphans gone). Covers every referenced source + both field casings (user profile/cover/preSuspension; conversation group + IMAGE/STICKER messages; reports + reportsArchive evidence; banners); **security guard proven for real** — a foreign-hosted (non-CDN) URL does NOT protect a same-named key (swept as orphan); all-folder sweep incl. starting-screens; non-array `imageUrls` tolerated. Per-folder list/delete `catch` = documented escape-hatch. **7/7 green ~2s; RED-sensitivity proven twice** (probe 1: skipped `r2.deleteObjects` → all 7 failed; probe 2: neutered `extractKey`→null → the 4 keep-object tests failed, the 3 no-keep tests correctly passed; both restored, prod byte-clean). eslint/prettier clean; ratchet tightened (orphanedStorage.test.js removed → jest.mock 185→184, jest.fn 217→216, mockResolvedValue 193→192). Commit `369a04077ae`. **⚠️ DISCOVERED PRODUCT RISK (flagged to operator, follow-up SHY candidate — NOT fixed here, out of test-migration scope):** orphanedStorage collects message/sticker keys for only the FIRST 30 conversations (`convsSnap.docs.slice(0, 30)`) but sweeps the ENTIRE `messages/` + `stickers/` R2 folders — so referenced media in the 31st+ conversation would be treated as orphans and DELETED (silent media data loss at scale). The test suite deliberately does NOT assert/bless this behaviour. **Remaining crons (2 biggest):** `backups`(228-src), `accountDeletion`(594-src).
- **2026-06-18 — SLICE 11 DONE (verified): `backups` (full DB backup → R2 — real Firestore emulator + real MinIO).** Replaced 26 firebase/r2/log Jest mocks (jest.mock×2, jest.doMock×3, jest.fn, mockResolvedValue) that asserted `putObject`/`deleteObjects` call SHAPES → 9 real tests that prove the disaster-recovery contract a mock cannot: a backed-up collection is **read back from the bucket and recoverable** (valid JSON, every doc with its injected `id`). Dev scope (3 collections, no subcollections) runs the naturally-loaded module: value-level users-collection read-back + manifest counts {users:2,config:1,counters:2} + `{date,manifest}` return; dev-subset-only (rooms/conversations/deviceBans + subcollections NOT backed up); empty→`[]` files; backwards-compat `backups/users/<date>.json` equals the full users backup; prune >7d on BOTH prefixes (old 2020 keys pruned, yesterday kept); prune skips unparseable date segments. **Production scope (27 collections + 11 subcollections) reached WITHOUT flipping NODE_ENV** — root cause: `r2.js` binds to MinIO *only* when `NODE_ENV==='local'` (and `firebase.js` routes to the emulator the same way), so there is **no single NODE_ENV that yields both the prod collection scope AND real MinIO**; the earlier `delete require.cache[...]`+re-`require` trick is a no-op under Jest (jest-runtime has its own registry, not Node's `require.cache`) — it returned the dev module and a half-broken r2 (`No value provided for input HTTP label: Key`). **Fix = backward-compatible DI seam** on `backups({ topLevelCollections, subcollections })` defaulting to the env-derived constants (slice-6 Option-A precedent applied to a config seam; both no-arg call sites in `admin-backup.js` + the route's constant exports unchanged; it's also a genuinely useful admin "back up everything / targeted" API, not test-only damage). Full-scope tests inject the module's own exported `ALL_*` constants → real expanded scope (reports/banners backed up) + real subcollection aggregation (`rooms_messages.json`: 3 docs across r1/r1/r2 with injected `parentId`+`id`) against the real stack. The one line not exercised = the runtime `NODE_ENV==='production'→ALL` ternary itself (its local→DEV sibling IS proven); documented. Dropped the 2 contrived "Firestore read throws" tests (manifest-omits-failed, users-`[]`-fallback) — a real `.get()` failure against a healthy emulator is not inducible without a mock = EPIC-0003 escape-hatch. **9/9 green ~2.5s; RED-sensitivity proven twice** (probe 1: dropped `id` injection in `backupCollection` → content read-back failed `Received: undefined`; probe 2: `parentId:'WRONG'` → subcollection aggregation failed; both restored, prod byte-clean vs HEAD — diff is ONLY the DI seam). eslint/prettier clean; ratchet tightened (backups.test.js removed → jest.mock 184→183, jest.fn 216→215, mockResolvedValue 192→191). Commit `781821821c7`. **Remaining cron (last + biggest):** `accountDeletion`(594-src — touches FCM/email/R2/Firestore; expect Option-A escape-hatches for un-inducible external delivery).
- **2026-06-18 — SLICE 12 DONE (verified): `accountDeletion` (GDPR hard-delete — the full real local stack: Firestore emulator + Firebase Auth emulator + real MinIO + real Mailpit).** The biggest cron (594-src). Corrected the handoff's assumption: there is **NO FCM** in this cron (imports are only `db, auth, FieldValue`) — every collaborator is real-inducible locally, so NO Option-A escape-hatch was needed for external delivery. Replaced 49 firebase/auth/email/r2/log mocks (asserting call SHAPES — "deleteUser was called", "`_path === 'reportsArchive/x'`") with **32 real tests** proving the erasure contract a mock cannot: data actually GONE/anonymised in the datastore, the credential actually removed from Auth (`auth.createUser` → run → `getUser` rejects `auth/user-not-found`), R2 media actually deleted in MinIO (foreign media preserved), and the deletion-complete email actually read back from Mailpit's HTTP API (subject + recipient). Main cron: pending-deletion processing + both `deletionExecuteAt` clauses (>0 and <=now, proven by a `0` doc + a future doc surviving) + the **10/run cap proven by 11 seeded users → exactly 1 backlog** + inactivity scheduling (enabled value-level: reason/scheduledAt/executeAt=scheduledAt+grace; disabled threshold 0; suspended-skip). Every `hardDeleteAccount` step value-level: conversations (1-on-1 full delete vs group arrayRemove + only-own userSettings/mutes), rooms (owned+subcollections vs non-owned arrayRemove), follower/following arrayRemove, giftRankings, reports/archive/appeals (reportedUserId/reporterId/+appeals userId; foreign kept), suggestions anonymise (submitterUid→0+flag, title preserved), votes via real **collectionGroup** + voteCount decrement (up→−1) + null-vote (delta 0) + **non-suggestion-parent guard** (decoy `decoys/x/votes` untouched), comments anonymise (authorUid→0, text replaced), subscriptions doc, notifications uid|recipientUid dedupe (foreign kept), Step 7 (biometricKeys + otp/emailMetrics by lowercased email + purchaseReceipts markedForDeletion+deletionScheduledAt), user doc + 5 subcollections, identityMap soft-delete clean **and** suspended standing, deviceBindings, Auth user, **audit log value-level** (HMAC `hashedUniqueId` == `createHmac(secret||'dev-audit-secret', uniqueId)`, zero PII — no uniqueId/email/firebaseUid — reason from deletionReason + 'unknown' fallback, standing, dataDeleted array), deviceBans preserved. **NO production change — cron byte-clean throughout (git diff HEAD empty).** Escape-hatches (documented, dropped): the fresh-read cancellation guards (`!exists`/`!deletionExecuteAt` on re-read — fire only on a real concurrent mutation between query and per-user re-read; no in-process hook) and the per-step try/catch logging branches (need a real failure injected into a healthy stack); loop-robustness is covered by the 11-user cap test. New stack facts proven: Auth emulator nuke endpoint `DELETE /emulator/v1/projects/demo-shytalk/accounts`; the emulator ignores collectionGroup index requirements (so the votes/comments cascades run for real); Mailpit API `GET/DELETE http://localhost:8025/api/v1/messages`. Caught + fixed a TEST bug (not product): `seedUserDoc({email:undefined})` → Admin SDK rejects `undefined` fields → strip undefined keys before `.set()`. **32/32 green ~6s; RED-sensitivity proven** (one run, 3 simultaneous probes: skipped `r2.deleteObjects` / skipped `auth.deleteUser` / flipped up-vote delta → exactly the Step-2, Step-11, Step-6b-decrement tests failed; all restored, prod byte-clean). eslint/prettier clean; ratchet tightened (accountDeletion.test.js removed → jest.mock 183→182, jest.fn 215→214, mockResolvedValue 191→190). Commit `00426547ac2`.
- **2026-06-18 — ZERO-GAP CLOSE-OUT (all 12 crons + sweep verified):** post-slice-12 audit of `tests/cron/*.test.js` for ANY remaining test double confirmed every cron integration test is real. Two grep hits: `index.unit.test.js` (unit-located → legitimately exempt, scheduler-wiring) and `expireTempIds.test.js` — the latter was already real (migrated SHY-0109) but its migration-rationale comment contained the literal token `jest.fn()`, which the substring-based ratchet scanner counted as phantom stub debt. Rephrased the comment ("in-process fakes") + regen → `expireTempIds.test.js` dropped from the baseline (jest.fn 214→213). Commit `fd973db841f`. **SHY-0120 cron-test migration COMPLETE — every cron under `src/cron/` is now tested real-only against the local stack.** Cumulative ratchet end state: **jest.mock 182, jest.fn 213, mockResolvedValue 190** (from 194/226/202 at slice 1 start). **NEXT = SHY-0120 ship:** full local express verify (`npm test`) → code-reviewer (0 findings) → push → CI (Detect Changes / Analyze JavaScript / PR Gate) → judgment-merge.
- **2026-06-18 — SLICE 2 DONE (verified): `subscriptions` (SuperShy expiry cron, 27 mock-lines → 9 real tests).** Seeds real users → runs the real cron → reads back: expired non-lifetime downgraded (isSuperShy=false, expiry/tier nulled), future/lifetime/non-supershy retained verbatim; `<=` boundary (expiry == now → downgraded); missing-tier (undefined ≠ lifetime → downgraded); multi-user batch; empty no-op. **9/9 green ~1.4s; RED-sensitivity proven** (zeroed the cron's `toExpire` → the downgrade test failed; restored). Dropped the prior mock test's impossible ">500 → 2 batches" case (the query's `.limit(500)` caps `toExpire` ≤ 500, so the chunk loop always runs once — the mock fabricated 501 docs past the limit); the truncation `log.warn` is observability-only (unmocked, fires only at exactly 500 rows — documented, not 500-seeded). eslint/prettier clean; ratchet tightened (subscriptions removed → jest.mock 193→192, jest.fn 225→224, mockResolvedValue 201→200).
