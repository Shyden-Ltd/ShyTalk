// QR code library placeholder — replace with actual qrcodejs (https://github.com/soldair/node-qrcode)
// This stub provides the QRCode constructor interface that portal.js expects.
// Usage: new QRCode(element, { text: 'otpauth://...', width: 200, height: 200 });
var QRCode = function(el, options) {
  if (el && options && options.text) {
    el.textContent = 'QR code placeholder — library not loaded';
  }
};
