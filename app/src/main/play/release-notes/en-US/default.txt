v0.58.4

build once, share artifacts, SonarCloud quality gate (#220)
- switch from Gradle ktlint/detekt to standalone ktlint (~30s vs ~2min).
-   No JDK or Gradle setup needed — just curl + run.
- now also runs detekt + :shared:jvmTest in a single Gradle
-   invocation. Uploads test-results, reports, and classes as artifacts
-   for SonarCloud to reuse.
