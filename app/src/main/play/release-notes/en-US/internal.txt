v0.57.0

fully offline local development environment
- One-command local dev with Docker (LiveKit, MinIO, Mailpit), Firebase
- Emulators, Express API auto-start, and Android APK build. Zero cloud
- services hit during development or CI testing.
- Key changes:
- LiveKit + MinIO (S3 storage) + Mailpit (email capture)
